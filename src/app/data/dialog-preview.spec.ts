import { DialogPreview } from './dialog-preview';

describe('DialogPreview', () => {
  it('should create an instance', () => {
    expect(new DialogPreview(0, '', '', '', '', null, 0)).toBeTruthy();
  });
});
